datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Mahasiswa {
  id    Int     @id @default(autoincrement())
  nama  String
  nim   String  @unique
}